# Asm (disambiguation)
The abbreviation "asm" is ambiguous and may refer to:

- [Asm(](asm-command.html), the command used for running a shell-independent assembly program.
- [Assembly](assembly.html), the other programming language available on the TI graphing calculators.

If an internal link led you here, you may wish to change the link to point directly to the intended article.
