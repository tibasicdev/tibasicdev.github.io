[[include inc:68k-command
|picture=newmat.png
|summary=returns a new matrix filled with 0s given the dimensions provided
|syntax= newMat(rows,cols)
newMat(rows,cols)

|location=
* Press 2nd MATH to enter the MATH popup
* Press 4 to enter the Matrix submenu
* Press F ( Alpha | on TI 89s) to select newMat(
|compatibility=TI-89/92/Voyager 200
|size=~8 byte(s)
]]

When integers are used as arguments, newMat() returns an empty matrix of the specified dimensions filled with zeros. When any type other than integers are used as as arguments, newMat returns a Data Type error. 

[[code]]
newMat(3,2)
          [[0,0][0,0][0,0]]

[[/code]]

+ Advanced Uses

------

Can be used to pre-allocate matrices for programs.

+ Optimization

+ Error Conditions

[[include inc:68k-error num=210|error=Data type|cause=the data type specified as a dimension is invalid. See above.]]

+ Related Commands