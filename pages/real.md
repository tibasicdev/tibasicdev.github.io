# Real (disambiguation)
The term "real" is ambiguous and may refer to:

- [real(](real-func.html), the complex number command returning the real part of a number.
- [Real](real-mode.html), the mode command that selects Real number mode.

If an internal link led you here, you may wish to change the link to point directly to the intended article.
